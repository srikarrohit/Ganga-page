$("ul.<%=@post.id%>").append("<%= escape_javascript(render partial: 'comments/comment', locals: { comment: @comment, post: @post}) %>")
$("ul.<%=@post.id%>").children("div.no_comment_div").remove()
$("div.<%=@post.id%>.follow_unfollow_container").html("<%= escape_javascript(render partial: 'shared/unfollow', locals: { post: @post}) %>")
