<%= form_for(:follow, url: follows_path, remote: true , html: {:class=>'float_left'} ) do |f| %>
  <div><%= hidden_field_tag :post_id, post.id %></div>
<%= f.submit "Follow Post", class: "follow follow_btn btn btn-xs btn-default btn-info" %>
  <% end %>
