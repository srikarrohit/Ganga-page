<%= form_for(:follow, url: follow_path(post.id), method: 'delete', remote: true, html: {:class=>'float_left'}) do |f| %>
  <%= f.submit "Unfollow Post", class: "unfollow unfollow_btn btn btn-xs btn-default" %>
  <% end %>
